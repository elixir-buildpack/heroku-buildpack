#!/usr/bin/env ruby
require_relative '../lib/elixir_buildpack'

ElixirBuildpack.initialize
ElixirBuildpack.build_dir = File.expand_path(ARGV[0])
ElixirBuildpack.cache_dir = File.expand_path(ARGV[1])
ElixirBuildpack.env_dir = File.expand_path(ARGV[2])
ElixirBuildpack.compile
